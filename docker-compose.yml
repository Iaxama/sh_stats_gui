services:
  app:
    build: .
    command: sleep infinity
    network_mode: host
    ports:
      - "5001:5001"
    env_file:
      - .github-sync-config
    volumes:
      - ./data:/app/data
      - .:/workspaces/sh_stats_gui:cached
